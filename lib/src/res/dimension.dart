import 'package:flutter/material.dart';

const PAD_SYM_H10 = EdgeInsets.symmetric(horizontal: 10.0);
const PAD_SYM_H20 = EdgeInsets.symmetric(horizontal: 20.0);
const PAD_SYM_H30 = EdgeInsets.symmetric(horizontal: 30.0);
const PAD_SYM_H40 = EdgeInsets.symmetric(horizontal: 40.0);
const PAD_SYM_H50 = EdgeInsets.symmetric(horizontal: 50.0);

const PAD_SYM_V10 = EdgeInsets.symmetric(vertical: 10);
const PAD_SYM_V20 = EdgeInsets.symmetric(vertical: 20);
const PAD_SYM_V30 = EdgeInsets.symmetric(vertical: 30);
const PAD_SYM_V40 = EdgeInsets.symmetric(vertical: 40);
const PAD_SYM_V50 = EdgeInsets.symmetric(vertical: 50);

const PAD_ONLY_T10 = EdgeInsets.only(top: 10);
const PAD_ONLY_T20 = EdgeInsets.only(top: 20);

const PAD_ASYM_H25_V10 = EdgeInsets.symmetric(horizontal: 25.0, vertical: 10.0);

const PAD_ALL_12 = EdgeInsets.all(12);

const SCREEN_PAD = EdgeInsets.symmetric(vertical: 15.0, horizontal: 30.0);
const SCREEN_PAD_INSIDE = EdgeInsets.symmetric(horizontal: 20.0);